{
  "name": "eazycloud-website",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs && node scripts/prerender.mjs",
    "check:site": "node scripts/check-built-site.mjs",
    "test": "npm run build && node --test tests/*.test.mjs && npm run check:site"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "esbuild": "0.25.6",
    "jsdom": "26.1.0"
  }
}
