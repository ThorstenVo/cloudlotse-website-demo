A single numbered workflow step — an orange circle dot, a description, and a trailing status word — sitting on a hairline row. Stack several to build a proof list.

```jsx
<ProofStep n={1} label="Capture requirements from the enquiry" status="recognised" />
<ProofStep n={2} label="Find relevant project information" status="connected" />
```
