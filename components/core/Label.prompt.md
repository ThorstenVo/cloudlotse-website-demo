The tiny uppercase eyebrow label that names a block. Muted grey by default; pass `color="var(--signal)"` for orange contexts.

```jsx
<Label>What becomes possible</Label>
<Label color="var(--signal)">Example / Nordwerk</Label>
```
