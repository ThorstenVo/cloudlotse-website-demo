The bilingual language switch from the CloudLotse topbar — a bordered pair where the active language is filled orange with ink text.

```jsx
<LanguageToggle languages={["DE", "EN"]} active="EN" onChange={setLang} />
```

Designed for dark chrome (border is white 45%). The active cell uses the signal orange.
